<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reset Password</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/auth.css">
    <style>
        body {
            background-image: url("/images/background/milad-fakurian-7wUorDiCMSU-unsplash.jpg");
        }

        .rightHeader p {
            font-size: 28px;
            font-weight: 600;
            letter-spacing: 2px;
            margin-top: 10px;
            text-align: center;
            color: white;
        }

        .rightBody label {
            font-size: 14px;
            color: white;
            margin-bottom: 5px;
        }

        .rightBody p {
            color: red;
            font-size: 14px;
            margin-bottom: 10px;
        }

        .rightBody button {
            width: 100%;
            height: 45px;
            border-radius: 50px;
            background-color: #992828;
            color: white;
            font-size: large;
            border: none;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        .rightBody button:hover {
            background-color: #7a1f1f;
        }
    </style>
</head>
<body>
    <%- include('../partials/header') %>
         
    <main>
        <div class="loginContainer">
            <div class="loginLeft">
                <img id="loginImage" src="/images/background/linevector.png" alt="Line Vector">
            </div>
            <div class="loginRight">
                <div class="rightHeader">
                    <img src="/images/background/U-Craft_logo.png" alt="U-Craft Logo" id="loginHeadLogo">
                    <p>Reset Your Password</p>
                </div>
                <div class="rightBody">
                    <form action="/resetPassword" method="post">
                        <!-- Hidden input to pass the email -->
                        <input type="hidden" name="email" value="<%= email %>">

                        <label for="password">New Password</label>
                        <% if (msg) { %>
                            <p><%= msg %></p>
                        <% } %>
                        <input type="password" id="password" name="password" placeholder="Enter your new password" required>

                        <label for="confirmPassword">Confirm Password</label>
                        <input type="password" id="confirmPassword" name="confirmPassword" placeholder="Confirm your new password" required>
                    
                        <button type="submit">Reset Password</button>
                    </form>
                </div>
            </div>
        </div>
    </main>
</body>
</html>